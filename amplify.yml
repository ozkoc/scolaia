version: 1
frontend:
  phases:
    preBuild:
      commands:
        - rm -rf node_modules dist .npm
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - "**/*"
